name: Deployment
on:
  push
# env:
#   MONGODB_DB_NAME: gha-demo #set env

jobs:
  pyjob:
    # env:
    #   MONGODB_CLUISTER_ADRESS:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: run_script
        run: "python my.py"