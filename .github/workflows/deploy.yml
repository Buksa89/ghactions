name: Deployment
on:
  push
# env:
#   MONGODB_DB_NAME: gha-demo #set env

jobs:
  pyjob:
    env:
      myenv: "myenv_var"
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: echo myenv
        run: echo "$myenv" #OR
      - name: echo myenv from env
        run: echo "${{env.myenv}}"
      - name: echo all envs
        run: echo "${{ toJSON(env)}}"
      # - name: run_script
      #   run: python my.py